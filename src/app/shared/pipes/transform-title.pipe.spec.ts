import { TransformTitlePipe } from './transform-title.pipe';

describe('TransformTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
