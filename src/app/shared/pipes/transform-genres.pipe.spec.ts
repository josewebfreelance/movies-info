import { TransformGenresPipe } from './transform-genres.pipe';

describe('TransformGenresPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformGenresPipe();
    expect(pipe).toBeTruthy();
  });
});
